# Dockerfile for UDACE Engine Service

FROM python:3.9-slim
WORKDIR /app
COPY . /app/
# RUN pip install --no-cache-dir -r requirements.txt # Placeholder
# CMD ["python", "data_acquisition.py"] # Or a dedicated worker script
CMD ["python", "data_acquisition.py"] # Using data_acquisition.py as placeholder entrypoint
